spring.application.name=msgDissapear

# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/msgdissapear
spring.datasource.username=postgres
spring.datasource.password=Uma@1927
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Scheduling (enabled via @EnableScheduling)
# Cleanup job runs every 60 seconds by default; override here if needed
message.cleanup.cron=0/60 * * * * *
